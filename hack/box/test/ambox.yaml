version: v1
sftpgo:
  host_key: /etc/ambox/sftpgo_host_key
processing:
  configs:
    - name: automated
      extends: automated
      config:
        normalize: access
    - name: demo
      extends: default
      config:
        virus_scanning: false
